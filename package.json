{
  "name": "vite-reactive-repro",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vue-tsc --noEmit || true && cp src/extra/mount/index.html dist/index.html",
    "host": "pnpm run build && cd src/extra/mount && docker compose up",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.0",
    "vue-tsc": "^0.29.8"
  }
}
